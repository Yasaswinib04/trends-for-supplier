{
  "name": "browser-scrapers",
  "version": "0.1.0",
  "private": true,
  "description": "Playwright-based marketplace scrapers for Kurti Trend Engine",
  "scripts": {
    "build": "tsc",
    "postinstall": "npx playwright install chromium 2>/dev/null || echo '⚠️ Playwright chromium download skipped (already installed or offline)'",
    "scrape": "npx tsx run.ts"
  },
  "dependencies": {
    "playwright": "^1.49.1",
    "dotenv": "^16.4.7"
  },
  "devDependencies": {
    "typescript": "^5.7.3",
    "tsx": "^4.19.2",
    "@types/node": "^22.10.5"
  }
}
